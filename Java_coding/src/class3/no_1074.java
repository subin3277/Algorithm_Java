package class3;

import java.util.Scanner;

public class no_1074 {

	public static void main(String[] args) {
		// TODO 자동 생성된 메소드 스텁
		Scanner in = new Scanner(System.in);
		int N = in.nextInt();
		int r = in.nextInt(); //행
		int c = in.nextInt(); //열
		double n = Math.pow(2, N)*Math.pow(2,N);
		
		while(r<Math.pow(2, N)) {
			
		}
	}

}
